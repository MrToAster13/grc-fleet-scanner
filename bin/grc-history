#!/usr/bin/env bash
# grc-history: list prior runs (reads history.db under the output dir).
here="$(cd "$(dirname "$(readlink -f "$0" 2>/dev/null || echo "$0")")" && pwd)"
. "$here/_grc-common.sh"
grc_setup
grc_run_tool history "$@"
